# Knowledge Base

This directory contains the knowledge base for the Hermes Agent.

## Structure

- `.md` files are treated as wiki entries
- Each file can have frontmatter with title, tags, and creation date

## Usage

Use `wiki_search` to find relevant entries.
Use `wiki_update` to create or update entries.
